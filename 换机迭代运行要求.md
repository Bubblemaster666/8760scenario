# 换机迭代运行要求（风光荷 8760：KMeans / V14 / Guo）

## 1. 保留代码版本（只保留这三份）
- `kmeans_markov_monthly_rebalance.py`
- `v14_conditional_wgan_gp.py`
- `guo_3dconv_stattn_conditional_wgan_gp.py`

## 2. 数据与目录要求
在项目根目录下保留数据文件夹：`8760电-风-光数据`
其中至少包含：
- `Load_data_8760.mat`
- `Onshore_wind_data_8760.mat`
- `Offshore_wind_data_8760.mat`
- `Utility_PV_data_8760.mat`
- `Distributed_PV_data_8760.mat`
- `CSP_data_8760.mat`

建议目录结构：
```text
project/
  .venv/
  8760电-风-光数据/
  kmeans_markov_monthly_rebalance.py
  v14_conditional_wgan_gp.py
  guo_3dconv_stattn_conditional_wgan_gp.py
```

## 3. 软件环境要求
- OS：Windows（PowerShell）
- Python：`3.10` 或 `3.11`
- 建议内存：`>=16GB`
- 可用 CPU 即可；有 GPU 可将 `--device cuda`（需匹配 CUDA 版 PyTorch）

## 4. 依赖安装
在项目根目录执行：
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install numpy pandas matplotlib scipy torch
```

## 5. 三个版本的运行命令

### 5.1 KMeans 基线
```powershell
.\.venv\Scripts\python kmeans_markov_monthly_rebalance.py --output-dir background_8760_outputs
```
输出关键文件：
- `background_8760_outputs\background_8760.csv`

### 5.2 V14（条件 WGAN-GP）
```powershell
.\.venv\Scripts\python v14_conditional_wgan_gp.py --output-dir gan_background_outputs_v14_best --device cpu
```

### 5.3 Guo 版（3DConv + 时空注意力 + 条件 WGAN-GP）
完整训练（慢）：
```powershell
.\.venv\Scripts\python guo_3dconv_stattn_conditional_wgan_gp.py --output-dir gan_background_outputs_guo_run --device cpu
```

基于已有 GAN 结果只做后处理迭代（快，推荐反复调参）：
```powershell
.\.venv\Scripts\python guo_3dconv_stattn_conditional_wgan_gp.py `
  --output-dir gan_background_outputs_guo_iter_xxx `
  --reuse-gan-csv gan_background_outputs_guo_run\gan_background_8760.csv `
  --refine-random-trials 1200 `
  --refine-objective max_ratio `
  --cost-match-top-n 60 `
  --cost-match-max-abs-shift 0.10 `
  --solar-zero-before-hour 6 `
  --solar-zero-after-hour 20 `
  --device cpu
```

## 6. 评价指标（六项，全部越小越好）
看输出文件：`compare_metric_ratios.csv`
- `Wasserstein(mean)`
- `JS(mean)`
- `ACF_MAE(mean)`
- `StdCurve_MAE(mean)`
- `Corr_L1`
- `Cost_RelErr`

对比口径：`ratio_gan_over_kmeans`
- `< 1`：GAN 优于 KMeans
- `= 1`：持平
- `> 1`：劣于 KMeans

建议主目标：
1. `max_ratio`（六项比值最大值）尽量小
2. 保证六项都 `< 1`
3. 在此基础上继续压 `StdCurve_MAE` 和 `Corr_L1`

## 7. 当前可参考的 Guo 最优基线（历史结果）
目录：`gan_background_outputs_guo_kmfix_opt2_stdfix`
`ratio_gan_over_kmeans`：
- Wasserstein: `0.7411`
- JS: `0.6940`
- ACF: `0.7043`
- StdCurve: `0.9541`
- Corr_L1: `0.9665`
- Cost: `0.0433`

## 8. 迭代建议（换机后直接照做）
1. 先跑一次 Guo 完整训练，得到 `gan_background_8760.csv`
2. 固定该 CSV，做 3~10 轮“仅后处理迭代”（`--reuse-gan-csv`）
3. 每轮改动 1~2 个参数（如 `refine-random-trials`、`cost-match-top-n`、`cost-match-max-abs-shift`）
4. 每轮保存独立输出目录，最后按 `max_ratio` 排序选最优

## 9. 常见问题
- 光伏夜间不为 0：确保参数 `--solar-zero-after-hour 20`（20 点后归零）
- 任务中断后有残留 Python 进程：
```powershell
Get-Process | Where-Object { $_.ProcessName -like '*python*' } | Stop-Process -Force
```
- 找不到 KMeans 文件：先运行第 5.1 节命令生成 `background_8760.csv`
